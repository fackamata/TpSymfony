{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('type_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('type_edit', {'id': type.id}) }}">edit</a>

    {{ include('admin/type/_delete_form.html.twig') }}
{% endblock %}
